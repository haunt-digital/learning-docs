<% content_for :title, @competency.title %>

<% content_for :header_bar do %>
  <%= render partial: 'competencies/progress', object: [@competency], as: :competencies %>
<% end %>

<%= render @competency %>
